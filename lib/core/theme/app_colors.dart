import 'package:flutter/material.dart';

class AppColors {
  // ================= Background =================
  static const Color backgroundColor = Color(0xFFFDFDFD);
  static const Color scaffoldColor = Color(0xFFF7F7F7);
  static const Color cardColor = Colors.white;

  // ================= Primary =================
  static const Color primaryColor = Color(0xFFF83758);
  static const Color secondaryColor = Color(0xFF3F8CFF);

  // ================= Text =================
  static const Color blackText = Color(0xFF1F1F1F);
  static const Color greyText = Color(0xFF8F959E);
  static const Color lightGreyText = Color(0xFFC4C4C4);
  static const Color whiteText = Colors.white;

  // ================= Buttons =================
  static const Color buttonBlue = Color(0xFF2979FF);
  static const Color buttonGreen = Color(0xFF3ECF8E);
  static const Color buttonPink = Color(0xFFF83758);

  // ================= Borders =================
  static const Color borderColor = Color(0xFFE5E5E5);
  static const Color dividerColor = Color(0xFFF0F0F0);

  // ================= Discount / Offer =================
  static const Color offerPink = Color(0xFFFFA6B6);
  static const Color offerLightPink = Color(0xFFFFE5EA);

  // ================= Rating =================
  static const Color starColor = Color(0xFFFFC107);

  // ================= Bottom Navigation =================
  static const Color navBarColor = Colors.white;
  static const Color navSelectedColor = Color(0xFFF83758);
  static const Color navUnselectedColor = Color(0xFF9E9E9E);

  // ================= Search =================
  static const Color searchFillColor = Color(0xFFF3F3F3);

  // ================= Containers =================
  static const Color lightContainer = Color(0xFFF8F8F8);

  // ================= Shadow =================
  static const Color shadowColor = Color(0x14000000);
}